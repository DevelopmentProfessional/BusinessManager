Inventory : Remove the notification that shows how many items are low on stokc. instead, that data can go into the dropdown. in fact, put the number of each item type in the dropdowns to the right of thier name., 
	Add Item : The min Count isnt changeable, remove the border on the description, Change the camera layout of the Add Item to look like the Ediut Item 
	
	Edit I(tem : Make the Min Couint editable 
	

	
clients: put the count of each tier on the dropdown name to the right of it 
	Add Client : remove the top right X in the header, remove the outkoine of the notes section and the address section. then put a border above it, move thje memebership stuff underneath the phone number field. make the phone number field abide byh the number coding, make the emial abide by the email code, 
	Edit client: format it like the add client, For the Services history and product history, put in reverse chronological where the most recent is near the bottom, if the service is upcoming, selecting it should open the appoiuntmenbt edit compoent to allow for that compoenent to be edited 
	Cart setup, When a client is connected to a cart, it should literally make that cart data go into the clients cart specifically. then selecting various clients should change what is in the cart at the time, currently  it lo9oks like the cqart is not persistent with the clinet. The only time the cart data should disappear is when the application is closed, but until a client is added, it can go into a cart object in cache on the local of that device. 

Employees:
	Requests: move the filter options into the footer in the first row, move the pending at the bottom and the approved at the top. add a time filter in the filters 
	Edit employee: 
		Details: Change the spacing between fields. Change the active checkbox to a toggle button that is just the button with the name active on it, only the style changes. 
		Benefits: Put the Dollar sign on salaery as an input group
		Signature: For the signature function, allow for a signature to be uploaded as a picture type as well as the ability to create one. The create gbutton should be floating to the bottom 
		Permission: the add permissions inpout should be fixed to the bottom, the table of permissions should float to the bottom, the delete button on the left . 

		
