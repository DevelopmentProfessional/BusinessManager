services:
  # Backend API Service
  - type: web
    name: lavish-beauty-api
    env: python
    plan: starter
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && python init_database.py && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: lavish-beauty-db
          property: connectionString
      - key: ALLOWED_ORIGINS
        value: https://lavishbeautyhairandnail.care,https://www.lavishbeautyhairandnail.care,https://lavish-beauty-app.onrender.com,http://localhost:5173
      - key: ENVIRONMENT
        value: production
    healthCheckPath: /health

  # Frontend Service
  - type: web
    name: lavish-beauty-app
    env: node
    plan: starter
    buildCommand: cd frontend && VITE_API_URL=https://lavish-beauty-api.onrender.com/api/v1 npm install && npm run build
    startCommand: cd frontend && npx serve dist -l $PORT -s
    envVars:
      - key: VITE_API_URL
        value: https://lavish-beauty-api.onrender.com/api/v1
      - key: VITE_HTTPS
        value: true
    healthCheckPath: /

databases:
  - name: lavish-beauty-db
    databaseName: lavishbeauty
    user: lavishbeauty
    plan: starter
 